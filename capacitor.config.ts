import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'click.buyandsell.shop',
  appName: 'Buyandsell',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
